"""Bundled model-history classification vocabularies and rules.

Loaded at runtime via ``importlib.resources`` (same pattern as
``anaplan_audit.data`` and ``anaplan_audit.transform.queries``), and
collected explicitly by the PyInstaller spec so they ship in the frozen
binary.
"""
